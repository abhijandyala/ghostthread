# Python 3.11+ (hydradb-sdk needs >=3.10, insforge needs >=3.11)
fastapi==0.140.8
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.0.1

# HydraDB - real-time grounding across the four connectors
hydradb-sdk==2.1.2

# Pipeshift is OpenAI-API-compatible; there is no first-party SDK on PyPI.
openai==2.49.0

# RocketRide Cloud pipeline client. Install at the venue - pin once confirmed.
# rocketride>=1.3.0
