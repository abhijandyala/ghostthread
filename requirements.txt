# Python 3.11+ (hydradb-sdk needs >=3.10, insforge needs >=3.11)
fastapi==0.140.8
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.0.1

# HydraDB - real-time grounding across the four connectors
hydradb-sdk==2.1.2

# Anthropic - both specialised models. Classification (extract.py) and code
# generation (fixgen.py) are different models on one credential, and both run
# under structured outputs, which the SDK exposes as output_config.format.
anthropic==0.120.2

# RocketRide Cloud pipeline client. Drives `rocketride start` and the
# scripts/validate_pipe.py catalogue check; authenticates from ROCKETRIDE_APIKEY.
rocketride==1.3.0
